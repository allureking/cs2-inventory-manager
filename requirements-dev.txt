# 开发/测试工具依赖（在 requirements.txt 之外额外需要的，仅本地开发）
# 安装: pip install -r requirements.txt -r requirements-dev.txt && pre-commit install
# 注：运行时依赖（含 python-multipart）已在 requirements.txt，无需在此重复。
pytest
pytest-cov
pre-commit
